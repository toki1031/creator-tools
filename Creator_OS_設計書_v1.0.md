# Creator OS 設計書 v1.0
**状態:** 実装開始可能  
**初期対象:** 偉人・教養系YouTube Shorts  
**最優先端末:** iPhone Safari
## 1. 文書の目的

本書は、Creator OSを実装可能な製品仕様へ落とし込むための基本設計書である。

対象は、YouTube Shortsを中心とした動画制作を、台本・読み上げ・画像・BGM・字幕・動画出力まで一つのWebアプリで支援するシステムである。

本書の役割は次の三点。
- 開発時の判断基準を統一する
- 最小構成から段階的に実装できるようにする
- 後から機能を増やしても作り直しを最小化する
## 2. プロダクト定義

製品名：Creator OS
コンセプト：素材を探す時間と、編集で迷う時間を減らすAI動画プロデューサー
初期ターゲット：偉人・教養系YouTube Shortsを制作する個人クリエイター
対応端末：iPhone Safariを最優先。PCブラウザにも対応
初期出力：9:16、最大60秒、1080×1920のMP4

基本思想：
- 無いなら作る
- クリエイターは「選ぶ」と「確認する」に集中する
- 字幕表示用テキストと音声用テキストを分離する
- 著作権・商用利用条件が不明な素材は組み込まない
- まずShortsに特化し、長尺編集は後から追加する
## 3. MVPの範囲

MVPは「台本からShorts動画を1本書き出せる」状態とする。

含める機能：
- プロジェクト作成・保存
- 台本入力・自然な語り口への整形
- 読み上げ辞書と音声用原稿生成
- 端末内蔵音声による試聴
- 画像アップロードと並び替え
- 無料BGMの選択または音源アップロード
- 字幕分割・編集
- 画像＋音声＋字幕＋BGMのプレビュー
- MP4書き出し

MVPに含めない機能：
- Creator OS内での画像生成
- 本格的な多段タイムライン
- 長尺4K動画
- YouTubeへの直接投稿
- AI作曲の本番実装
- 複数人共同編集
## 4. ユーザーフロー

新規プロジェクト作成
→ 台本入力
→ 語り口を整える
→ 読み方を確認・修正
→ 音声を試聴または生成
→ 画像をアップロード
→ シーンへ画像を割り当て
→ BGMを選択
→ 字幕を調整
→ 全体プレビュー
→ MP4を書き出す
## 5. 画面一覧

|ID|画面|責務|
|---|---|---|
|S01|ホーム／ダッシュボード|新規プロジェクト、最近のプロジェクト、各ツールへの入口|
|S02|新規プロジェクト|タイトル、媒体、比率、尺、ジャンルを設定|
|S03|台本Studio|台本入力、自然化、構成分割、文字数・推定尺|
|S04|読み方確認|固有名詞抽出、読み辞書、一時修正、部分試聴|
|S05|ナレーション|音声選択、速度、高さ、スタイル、全文試聴|
|S06|画像／シーン|画像アップロード、シーン割当、表示順、表示時間|
|S07|BGM|無料BGM、アップロード、音量、ループ、ダッキング|
|S08|字幕|自動分割、表示テキスト、タイミング、位置、スタイル|
|S09|動画プレビュー|縦画面プレビュー、再生、シーン移動、警告表示|
|S10|書き出し|解像度、FPS、ファイル名、MP4生成、進捗表示|
|S11|設定|辞書管理、既定音声、既定字幕、BGM利用条件|

## 6. 主要機能仕様

|ID|機能|受入条件|優先度|
|---|---|---|---|
|F-001|プロジェクト保存|端末内保存。初期はIndexedDB。自動保存を行う|必須|
|F-002|台本自然化|改行、句読点、間、息継ぎ単位を調整する。意味は変えない|必須|
|F-003|推定尺|文字数と話速から動画尺を算出する|必須|
|F-004|表示原稿／音声原稿分離|字幕は漢字、音声は読み仮名置換後の文章を使用|必須|
|F-005|読み辞書|表記・読み・カテゴリ・優先度・適用範囲を保存|必須|
|F-006|部分試聴|選択文または1文だけを読み上げる|必須|
|F-007|端末音声列挙|利用可能な日本語音声を性別固定せず一覧化|必須|
|F-008|画像アップロード|JPEG/PNG/WebP、並び替え、削除|必須|
|F-009|シーン自動分割|台本を意味単位に分け、シーン候補を作る|推奨|
|F-010|BGMライブラリ|利用条件が記録された無料BGMのみ登録|必須|
|F-011|自動ダッキング|ナレーション中にBGM音量を下げる|必須|
|F-012|字幕分割|文字数と句読点で字幕単位を作る|必須|
|F-013|字幕タイミング|初期は均等割り。音声生成後は音声尺に再配分|必須|
|F-014|プレビュー|画像・字幕・音声・BGMを同期再生|必須|
|F-015|MP4出力|9:16、1080×1920、30fps、H.264/AACを標準|必須|
|F-016|画像プロンプト出力|ChatGPTへ渡せるシーン別プロンプトを生成|推奨|

### 6.1 読み上げ辞書仕様

辞書エントリーは以下を保持する。

```ts
type PronunciationEntry = {
  id: string;
  surface: string;        // 本田宗一郎
  reading: string;        // ほんだ そういちろう
  category: "person" | "place" | "term" | "brand" | "custom";
  scope: "global" | "genre" | "project";
  genreId?: string;
  projectId?: string;
  priority: number;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
};
```

適用順序は、プロジェクト辞書 → ジャンル辞書 → 全体辞書。長い表記を先に置換する。字幕用原稿は変更しない。

### 6.2 BGM仕様

無料BGMは、音声ファイルだけでなく利用条件メタデータを必須とする。

```ts
type BgmTrack = {
  id: string;
  title: string;
  category: string[];
  fileUrl: string;
  durationSec: number;
  commercialUse: boolean;
  monetizationAllowed: boolean;
  attributionRequired: boolean;
  attributionText?: string;
  sourceName: string;
  licenseUrl?: string;
};
```

初期プリセット：教養、歴史・重厚、挑戦、感動、希望、緊張、和風、ドキュメンタリー。

### 6.3 動画出力仕様

- コンテナ：MP4
- 映像：H.264
- 音声：AAC
- 解像度：1080×1920
- FPS：30
- 最大尺：60秒（MVP）
- 画像：coverで切り抜き。安全領域を考慮
- BGM：動画尺に合わせてループし、末尾でフェードアウト
- 処理失敗時は、原因と再試行方法を表示

## 7. データモデル

```ts
type CreatorProject = {
  id: string;
  title: string;
  genre: "great-person" | "education" | "fortune" | "bgm" | "other";
  platform: "youtube-shorts" | "instagram-reels" | "tiktok";
  aspectRatio: "9:16";
  targetDurationSec: number;
  displayScript: string;
  speechScript: string;
  scenes: Scene[];
  narration: NarrationSettings;
  bgm?: ProjectBgm;
  subtitleStyle: SubtitleStyle;
  createdAt: string;
  updatedAt: string;
  schemaVersion: number;
};

type Scene = {
  id: string;
  order: number;
  text: string;
  speechText: string;
  imageAssetId?: string;
  startSec: number;
  endSec: number;
  motion: "none" | "zoom-in" | "zoom-out" | "pan-left" | "pan-right";
  transition: "cut" | "fade";
};
```

保存先はMVPではIndexedDB。プロジェクトのJSONエクスポート／インポートを必須とし、ブラウザデータ消失に備える。

## 8. システム構成

### 8.1 MVP

- ホスティング：Cloudflare Pages
- ソース管理：GitHub
- UI：HTML/CSS/TypeScript（段階的にReactまたはVueへ移行可）
- ローカル保存：IndexedDB
- 音声試聴：Web Speech API
- 音声・動画処理：Web Audio API、Canvas、ffmpeg.wasm
- PWA：manifest + service worker

### 8.2 推奨ディレクトリ

```text
creator-os/
├── docs/
├── public/
│   ├── bgm/
│   └── icons/
├── src/
│   ├── app/
│   ├── components/
│   ├── features/
│   │   ├── projects/
│   │   ├── scripts/
│   │   ├── pronunciation/
│   │   ├── narration/
│   │   ├── scenes/
│   │   ├── bgm/
│   │   ├── subtitles/
│   │   └── export/
│   ├── services/
│   ├── storage/
│   ├── types/
│   └── utils/
├── tests/
├── package.json
└── README.md
```

### 8.3 将来のサーバー構成

自然なAI音声、MP3生成、長尺動画、AI作曲はサーバー処理へ移す。APIキーはブラウザへ埋め込まない。Cloudflare Workers等で認証・利用量制御を行う。

## 9. 非機能要件

- iPhone Safariで主要操作が完結する
- タップ領域は44px以上
- 自動保存は入力後1秒以内に実行
- 60秒プロジェクトを再読込して復元できる
- 重要操作の前に確認を表示する
- 端末外へデータを送信する場合は明示する
- APIキーをクライアント側へ保存しない
- BGMごとに利用条件を確認できる
- 日本語の文字化け、縦長画面でのはみ出しを防ぐ
- 書き出し中に画面がスリープしても再試行できる設計にする

## 10. 開発ロードマップ

|段階|目的|成果物|目安|
|---|---|---|---|
|Sprint 0|基盤整理|既存v2をバックアップ、TypeScript構成、IndexedDB、ルーティング|1週間|
|Sprint 1|台本・辞書|台本Studio、表示原稿／音声原稿、読み辞書、部分試聴|1〜2週間|
|Sprint 2|シーン・画像|シーン分割、画像アップロード、並び替え、プレビュー|1〜2週間|
|Sprint 3|BGM・字幕|無料BGM登録、利用条件、字幕分割、音量、ダッキング|1〜2週間|
|Sprint 4|動画出力|Canvas描画、音声合成、ffmpeg.wasm、MP4出力|2〜3週間|
|Sprint 5|品質改善|iPhone実機検証、エラー処理、PWA、テンプレート|1〜2週間|

## 11. テスト・完了条件

### MVP完了条件

1. iPhone Safariで新規プロジェクトを作成できる。
2. 台本を閉じても再度開いた際に復元できる。
3. 「本田宗一郎」など登録語を音声原稿だけへ置換できる。
4. 端末内の複数音声を選択し、部分試聴できる。
5. 5枚以上の画像をアップロードし、順番を変更できる。
6. BGMの利用条件を画面で確認できる。
7. 字幕を編集し、プレビューへ反映できる。
8. 60秒以内の1080×1920 MP4を出力できる。
9. 出力失敗時に、ユーザーが次に行う操作を表示する。
10. プロジェクトJSONを書き出し、別端末で読み込める。

### 最低限のテスト

- 単体：辞書適用、字幕分割、時間配分、BGMループ
- 結合：プロジェクト保存から復元、プレビュー同期、MP4生成
- 実機：iPhone Safari、画面回転、バックグラウンド復帰、低メモリ

## 12. リスクと対策

|リスク|対策|
|---|---|
|Safariで動画処理が重い|MVPは60秒・画像中心に制限。低解像度プレビューを使用|
|Web Speech APIの音声品質差|試聴用途とし、将来はサーバー型AI音声へ移行|
|MP3を直接得られない|試聴と本番音声生成を分離。初期は外部音声アップロードも許可|
|読みの誤り|階層辞書、長い語優先、部分試聴、プロジェクト辞書|
|無料BGMの規約変更|出典・規約URL・確認日を保持し、定期再確認|
|ブラウザデータ消失|JSONバックアップ、将来はアカウント同期|
|API費用増大|利用量制限、キャッシュ、機能ごとの課金上限|

## 13. 採用しない設計と理由

- 初期から本格タイムライン編集：開発量が大きく、Shorts自動生成の価値検証が遅れるため。
- 初期から画像生成APIを内蔵：費用・規約・人物一貫性の管理が増えるため。画像生成は当面ChatGPTを使用。
- 出典不明の無料BGMを同梱：収益化動画での権利問題を避けるため。
- APIキーを利用者のブラウザに直書き：漏えいの危険があるため。
- 全機能を単一HTMLへ追加し続ける：保守性とテスト性が低下するため、次段階から機能単位へ分割する。

## 14. 次の実装タスク

### 最優先：Sprint 0

1. 現在のGitHubリポジトリをバックアップする。
2. `main`とは別に`develop`ブランチを作成する。
3. Vite + TypeScriptで新しいアプリ雛形を作る。
4. 既存の読み上げ・台本整形・字幕分割を移植する。
5. IndexedDBのプロジェクト保存層を作る。
6. プロジェクトJSONのエクスポート／インポートを追加する。
7. iPhone Safariで動作確認する。

### Sprint 1の最初のユーザーストーリー

> クリエイターとして、字幕には漢字を表示したまま、音声だけ正しい読みへ変更したい。そうすれば読み間違いのない動画を作れる。

受入条件：
- 表記と読みを登録できる
- 登録語が音声原稿だけに反映される
- 辞書を編集・削除できる
- 全体、ジャンル、プロジェクトの適用範囲を選べる
- 1文だけ試聴できる
